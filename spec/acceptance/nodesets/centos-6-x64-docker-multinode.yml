HOSTS:
  web:
    roles:
      - master
      - agent
      - web
    platform: el-6-x86_64
    hypervisor: docker
    image: centos:6
    docker_preserve_image: true
    docker_cmd:
      - '/sbin/init'
    docker_image_commands:
      - 'yum install -y tar wget cronie'
    docker_container_name: 'xdmod-web'
  db:
    roles:
      - agent
      - db
    platform: el-6-x86_64
    hypervisor: docker
    image: centos:6
    docker_preserve_image: true
    docker_cmd:
      - '/sbin/init'
    docker_image_commands:
      - 'yum install -y tar wget cronie'
    docker_container_name: 'xdmod-db'
  akrr:
    roles:
      - agent
      - akrr
    platform: el-6-x86_64
    hypervisor: docker
    image: centos:6
    docker_preserve_image: true
    docker_cmd:
      - '/sbin/init'
    docker_image_commands:
      - 'yum install -y tar wget cronie'
    docker_container_name: 'xdmod-akrr'
  supremm:
    roles:
      - agent
      - supremm
    platform: el-6-x86_64
    hypervisor: docker
    image: centos:6
    docker_preserve_image: true
    docker_cmd:
      - '/sbin/init'
    docker_image_commands:
      - 'yum install -y tar wget cronie'
    docker_container_name: 'xdmod-supremm'
  compute:
    roles:
      - agent
      - compute
    platform: el-6-x86_64
    hypervisor: docker
    image: centos:6
    docker_preserve_image: true
    docker_cmd:
      - '/sbin/init'
    docker_image_commands:
      - 'yum install -y tar wget cronie'
    docker_container_name: 'xdmod-compute'
CONFIG:
  log_level: debug
  type: foss
