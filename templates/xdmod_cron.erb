# Every morning at 3:00 AM -- run the report scheduler
0 3 * * * root /usr/bin/php /usr/lib/xdmod/report_schedule_manager.php >/dev/null

# Shred and ingest:
0 1 * * * root <%= @shredder_command_real %> && /usr/bin/xdmod-ingestor --quiet
