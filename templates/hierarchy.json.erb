<% hierarchy_levels = scope.lookupvar('xdmod::hierarchy_levels') || {} -%>
<%-
levels = {}
hierarchy_levels.each_pair do |level, data|
  data.each_pair do |k, v|
    levels["#{level}_level_#{k}"] = v
  end
end
%>
<%= JSON.pretty_generate(levels) %>
